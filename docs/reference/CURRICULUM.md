# COSLAB 커리큘럼: C로 배우는 컴퓨터 원리

> 이 프로젝트는 C 문법이 아닌 **컴퓨터의 원리**를 가르치는 것이 목표

---

## 📚 학습 목차

### Part 1: 컴퓨터는 데이터를 어떻게 저장하는가

```
1.1 비트와 바이트
    - 0과 1로 모든 것을 표현하기
    - 1바이트 = 8비트 = 256가지 표현
    - 왜 16진수(hex)를 쓰는가

1.2 숫자의 표현
    - 양수: 그냥 2진수
    - 음수: 2의 보수 (Two's complement)
    - 실수: 부동소수점 (IEEE 754)
    - 오버플로우가 일어나는 이유

1.3 엔디안 (Endianness)
    - 리틀 엔디안 vs 빅 엔디안
    - 왜 int 5가 [05 00 00 00]으로 저장되는가
    - 네트워크 바이트 순서
```

### Part 2: 메모리의 구조

```
2.1 메모리 주소란?
    - 메모리 = 거대한 바이트 배열
    - 주소 = 배열의 인덱스
    - 32비트 vs 64비트 주소 공간

2.2 프로세스 메모리 레이아웃
    ┌─────────────┐ 높은 주소
    │    Stack    │ ← 지역변수, 함수호출
    │      ↓      │
    │             │
    │      ↑      │
    │    Heap     │ ← malloc
    ├─────────────┤
    │    BSS      │ ← 초기화 안 된 전역
    │    Data     │ ← 초기화된 전역
    │    Text     │ ← 코드 (기계어)
    └─────────────┘ 낮은 주소

2.3 스택 (Stack)
    - LIFO 구조
    - 함수 호출 시 스택 프레임 생성
    - 지역변수가 함수 끝나면 사라지는 이유
    - 스택 오버플로우

2.4 힙 (Heap)
    - 동적 할당 영역
    - 프로그래머가 직접 관리
    - 메모리 누수 (Memory Leak)
    - 힙 단편화 (Fragmentation)
```

### Part 3: 포인터의 본질

```
3.1 포인터 = 주소를 담는 변수
    - int x = 5;      → 메모리에 5 저장
    - int *p = &x;    → x의 주소 저장
    - *p = 10;        → 주소 따라가서 값 변경

3.2 포인터 연산
    - p + 1 이 실제로 몇 바이트 이동하는가
    - 배열과 포인터의 관계
    - arr[i] == *(arr + i)

3.3 다중 포인터
    - int **pp; 가 의미하는 것
    - 포인터의 포인터가 필요한 이유

3.4 포인터와 함수
    - Call by Value vs Call by Reference
    - 함수에서 값을 "돌려주는" 방법
    - swap 함수가 포인터 없이 안 되는 이유

3.5 위험한 포인터
    - Dangling Pointer (해제된 메모리 참조)
    - Wild Pointer (초기화 안 된 포인터)
    - NULL Pointer Dereference
    - Buffer Overflow
```

### Part 4: 동적 메모리 할당의 원리

```
4.1 malloc의 내부 동작
    - 시스템 콜: brk(), mmap()
    - Free List 관리
    - 메모리 블록 헤더 (크기, 사용중 여부)

4.2 free의 내부 동작
    - 블록을 Free List에 반환
    - 인접 블록 병합 (Coalescing)
    - Double Free 문제

4.3 메모리 할당 전략
    - First Fit, Best Fit, Worst Fit
    - 단편화 문제
    - 메모리 풀 (Memory Pool)

4.4 가비지 컬렉션 (다른 언어와 비교)
    - C: 수동 관리 (free 직접 호출)
    - Java/Python: 자동 GC
    - Reference Counting vs Mark-and-Sweep
    - 왜 C는 GC가 없는가
```

### Part 5: 배열과 구조체의 메모리

```
5.1 배열의 메모리 배치
    - 연속된 메모리 공간
    - arr[i]의 주소 = arr + (i * sizeof(element))
    - 2차원 배열의 메모리 (Row-major)

5.2 구조체와 메모리 정렬
    - Padding과 Alignment
    - 왜 struct 크기가 예상과 다른가
    - #pragma pack

5.3 Union
    - 같은 메모리를 다른 타입으로 해석
    - Type Punning
```

### Part 6: 함수 호출의 원리

```
6.1 스택 프레임
    - Return Address
    - Saved Registers
    - Local Variables
    - Arguments

6.2 호출 규약 (Calling Convention)
    - 인자 전달 방법 (레지스터 vs 스택)
    - 반환값 전달
    - cdecl, stdcall, fastcall

6.3 재귀와 스택
    - 재귀 호출 시 스택 변화
    - 꼬리 재귀 최적화 (Tail Call)
    - 스택 오버플로우 방지
```

### Part 7: CPU와 레지스터

```
7.1 레지스터란?
    - CPU 내부의 초고속 저장소
    - 범용 레지스터 (RAX, RBX, RCX, RDX)
    - 특수 레지스터 (RSP, RBP, RIP)

7.2 명령어 실행 과정
    - Fetch → Decode → Execute
    - 어셈블리 맛보기
    - C 코드 → 어셈블리 변환 보기

7.3 플래그 레지스터
    - Zero Flag, Carry Flag, Overflow Flag
    - 조건문이 어떻게 동작하는가
```

### Part 8: 운영체제와 메모리

```
8.1 가상 메모리
    - 물리 주소 vs 가상 주소
    - 왜 모든 프로세스가 같은 주소를 쓸 수 있는가
    - Page Table

8.2 페이징
    - 페이지와 프레임
    - Page Fault
    - Swapping

8.3 메모리 보호
    - 다른 프로세스 메모리 접근 불가
    - Segmentation Fault의 원인
    - DEP (Data Execution Prevention)
```

### Part 9: 실전 디버깅

```
9.1 일반적인 메모리 버그
    - Buffer Overflow
    - Use After Free
    - Memory Leak
    - Uninitialized Variable

9.2 디버깅 도구
    - GDB 기본
    - Valgrind로 메모리 누수 찾기
    - AddressSanitizer
```

---

## 🎯 앱에서 시각화할 것

| 파트 | 시각화 |
|------|--------|
| 1. 데이터 저장 | 바이트 박스, 2진수/16진수 표시 |
| 2. 메모리 구조 | 스택/힙 레이아웃 |
| 3. 포인터 | 화살표로 연결 |
| 4. malloc/free | 힙 블록 생성/삭제 애니메이션 |
| 5. 배열/구조체 | 연속 메모리 표시 |
| 6. 함수 호출 | 스택 프레임 쌓이는 모습 |
| 7. 레지스터 | RSP, RBP 변화 |

---

## 🚀 MVP 우선순위

1. **Part 3: 포인터** - 가장 핵심!
2. **Part 2: 메모리 구조** - 스택/힙 이해
3. **Part 4: malloc/free** - 동적 할당
4. **Part 1: 데이터 표현** - 바이트 시각화
